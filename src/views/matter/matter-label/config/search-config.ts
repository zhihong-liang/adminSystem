const config: CnPage.SearchProps = {
  model: {},
  labelPosition: 'left',
  items: [{ label: '标签名称', prop: 'lableName', component: 'input' }],
  colSpan: 6,
  footerSpan: 0,
  labelWidth: 120
}

export default config
